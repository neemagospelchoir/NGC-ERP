{
  "name": "@ngc/services",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic business/domain logic, shared by apps/web, apps/mobile, and apps/edge-functions (ARCHITECTURE.md S3). Phase 6 added src/auth; Phase 7.1 adds src/departments, src/families, src/members; later phases add one directory per module.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "tsx --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@ngc/db": "workspace:*",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
