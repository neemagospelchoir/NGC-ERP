{
  "name": "@ngc/db",
  "version": "0.1.0",
  "private": true,
  "description": "Supabase client factories (browser + server/SSR) and the shared Database TypeScript type for the NGC ERP schema.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "gen:types": "node scripts/generate-types.mjs"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.45.0",
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "pg": "^8.12.0",
    "@types/pg": "^8.11.0"
  }
}
